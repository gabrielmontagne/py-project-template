from setuptools import setup

setup(
    name='[+name+]',
    author='[+author+]',
    author_email='[+email+]',
    license='MIT',
    version='[+version+]',
    entry_points={
        'console_scripts': [
            '[+name+] = [+name+].__main__:main'
        ]
    }
)
